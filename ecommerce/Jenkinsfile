pipeline {
    agent any

    tools {
        maven 'Maven-3.9'
        jdk 'jdk-21'
    }

    stages {
        stage('Debug') {
            steps {
                echo '✅ Jenkinsfile is being executed'
            }
        }

        stage('Run Tests') {
            steps {
                bat 'mvn clean test'
            }
        }
    }
}
